const fs = require('fs-jetpack');
const sharp = require('sharp');

const files = fs.find('./src/assets/categories', { matching: ['*.png', '*.jpg', '*.gif'] });

files.forEach((file, i) => {
    console.log('file', file);
    sharp(file)
    .resize(400)
    .toFile(`optimg/${i}.jpg`)
});